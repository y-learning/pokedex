name: pokedex
description: A new Flutter application.

version: 1.0.0+1

environment:
  sdk: ">=2.2.2 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^0.1.2
  flutter_screenutil: ^0.6.0
  auto_size_text: ^2.1.0
  font_awesome_flutter: ^8.5.0
  flutter_svg: ^0.14.0

dev_dependencies:
  flutter_test:
    sdk: flutter


flutter:
  uses-material-design: true
  assets:
    - images/
    - images/evolution_icons/
    - images/types/
    - images/pokemons/
    - images/items/
